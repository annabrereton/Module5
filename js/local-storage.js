// To put a value into localStorage:
// const username = 'cuthbert'
// localStorage.setItem('username', username)

// To get a value from localStorage:
const username = localStorage.getItem('username')
console.log(username)

// sessionStorage the same but replace local with session
// sessionStorage will end with browser session while localStorage persists
